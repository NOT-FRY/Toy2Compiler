proc incrementa(valore: real):
    valore ^= valore + 1;
endproc

proc main():

    var x : integer; y: boolean; pi ^= 3.14;\
    incrementa(pi);
    -->!"Valore di pi non incrementato perche' non viene passato il riferimento: "$(pi);
endproc