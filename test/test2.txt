func f() -> real:
    var a,b^=2.3,3.2;\
    return a;
endfunc

proc main():
    var e:boolean;\
    a ^= f();
    var a,d,f:real;\
    e ^= ((5 *4 +3) > d + 3) && d > 1;
    somma(f());
endproc

proc somma(out a:real):
    a ^= 1.1;
endproc